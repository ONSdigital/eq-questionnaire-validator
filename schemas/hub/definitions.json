{
  "$id": "https://eq.ons.gov.uk/hub/definitions.json",
  "hub": {
    "type": "object",
    "properties": {
      "enabled": {
        "description": "Whether or not the hub and spoke pattern is enabled for this questionnaire.",
        "type": "boolean"
      },
      "required_completed_sections": {
        "description": "The sections that need to be complete prior to the hub displaying",
        "type": "array",
        "items": {
          "type": "string"
        }
      },
      "complete": {
        "type": "object",
        "description": "The title/guidance that will appear on the hub, when the questionnaire is complete",
        "anyOf": [
          {
            "required": ["title"]
          },
          {
            "required": ["guidance"]
          }
        ]
      },
      "incomplete": {
        "type": "object",
        "description": "The guidance that will appear on the hub, when the questionnaire is incomplete.",
        "properties": {
          "guidance": {
            "type": "string"
          }
        },
        "additionalProperties": false
      },
      "submission": {
        "type": "object",
        "properties": {
          "button": {
            "type": "string",
            "description": "The text that appears on the submit button, when the questionnaire is ready to be submitted"
          }
        },
        "additionalProperties": false
      }
    },
    "required": ["enabled"],
    "additionalProperties": false
  }
}
